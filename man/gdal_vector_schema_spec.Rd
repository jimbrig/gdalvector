% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_vector_schema.R
\name{gdal_vector_schema_spec}
\alias{gdal_vector_schema_spec}
\title{Build a GDAL Vector Schema Spec}
\usage{
gdal_vector_schema_spec(
  dsn,
  layer = gdal_vector_layer(dsn),
  keep_fid = TRUE,
  fid_name = "source_fid",
  fid_type = "Integer64",
  geom_name = "geometry",
  ...
)
}
\arguments{
\item{dsn}{The data source name (path or connection string).}

\item{layer}{The layer name. Defaults to the first layer via \code{\link[=gdal_vector_layer]{gdal_vector_layer()}}.}

\item{keep_fid}{Logical; whether to seed a synthetic FID row that carries the (virtual) feature id through as an
attribute via \verb{CAST(rowid AS INTEGER) AS <fid_name>}. Many drivers (e.g. GeoPackage) expose the FID as a virtual
column that is \emph{not} a real field, so this is the reliable way to retain it. Defaults to \code{TRUE}.}

\item{fid_name, fid_type}{The target name and type for the synthetic FID row. Defaults to \code{"source_fid"} and
\code{"Integer64"}.}

\item{geom_name}{The target name for the geometry field. Defaults to \code{"geometry"}.}

\item{...}{Passed on to \code{\link[=gdal_vector_schema]{gdal_vector_schema()}}.}
}
\value{
A \code{gdal_vector_schema_spec} tibble.
}
\description{
Builds the editable "schema spec" that drives \code{\link[=gdal_vector_schema_pipeline_args]{gdal_vector_schema_pipeline_args()}} and the individual emitters. It
is a single \code{\link[tibble:tibble]{tibble::tibble()}} with exactly one row per \strong{output} field, seeded from the data source's exported
schema (\code{\link[=gdal_vector_schema]{gdal_vector_schema()}}) so that, by default, the target schema is identical to the source schema.

You then edit the returned tibble - toggle \code{keep}, change \code{target_name} (rename), change \code{target_type} (retype),
and/or reorder via \code{order} - and hand it to the emitters. The emitters inspect the diff between the source and
target columns to decide which pipeline steps are actually required (see \code{\link[=gdal_vector_schema_pipeline_args]{gdal_vector_schema_pipeline_args()}}).

The spec columns are:

\describe{
\item{\code{order}}{Integer output ordering (lower comes first).}
\item{\code{role}}{One of \code{"fid"}, \code{"attribute"}, or \code{"geometry"}.}
\item{\code{keep}}{Logical; whether the field is carried into the output.}
\item{\code{source_name}}{The field name in the source layer (\code{"rowid"} for the synthetic FID row).}
\item{\code{source_type}}{The source \code{field_type} (see \code{\link[=gdal_vector_type_map]{gdal_vector_type_map()}}).}
\item{\code{source_subtype}}{The source subtype, e.g. \code{"JSON"}, else \code{"None"}.}
\item{\code{nullable}}{Logical; source nullability.}
\item{\code{target_name}}{The output field name (seeded from \code{source_name}).}
\item{\code{target_type}}{The output \code{field_type} (seeded from \code{source_type}).}
}
}
