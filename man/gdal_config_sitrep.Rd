% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_config.R
\name{gdal_config_sitrep}
\alias{gdal_config_sitrep}
\title{GDAL Configuration Situational Report}
\usage{
gdal_config_sitrep()
}
\value{
A \code{gdal_config_sitrep} object: \code{options} (tibble: \code{key}, \code{effective}, \code{envvar}, \code{set},
\code{source}), \code{set} (the active session payload), \code{vsi} (path-bound options per prefix), \code{file}
(the discovered config file), and \code{time}. Has cli \code{format()}/\code{print()} and
\code{\link[tibble:as_tibble]{tibble::as_tibble()}} methods.
}
\description{
A live, provenance-attributed report of the effective GDAL configuration landscape: for each
key with a visible signal, the effective value (read live from GDAL - the single source of
truth), the environment-variable value, what this package pinned, and a best-effort \code{source}
attribution:
\itemize{
\item \code{gdalvector}: currently pinned by this package (\code{\link[=gdal_config_set]{gdal_config_set()}}).
\item \code{envvar}: resolved from an environment variable.
\item \code{config_file}: matches a value declared in the discovered \link[=gdal_config_file]{GDAL config file}.
\item \code{external}: set in GDAL's in-memory store by something else (another package, a direct
\code{\link[gdalraster:set_config_option]{gdalraster::set_config_option()}} call, ...).
\item \code{unset}: no value in effect.
}

Coverage spans keys pinned by the package, GDAL-relevant environment variables, config-file
entries, and a probe of the full known-option universe (runtime VSI metadata, driver metadata,
and the curated core names), so externally set in-memory values for any documented key are
discovered too. Secret-bearing values are redacted in printed output. A baseline sitrep taken
at package load is stashed internally ("the world as gdalvector found it").
}
\examples{
\dontrun{
gdal_config_sitrep()
tibble::as_tibble(gdal_config_sitrep())
}
}
\seealso{
\code{\link[=gdal_config_active]{gdal_config_active()}}, \code{\link[=gdal_config_file]{gdal_config_file()}}, \code{\link[=gdal_config_set]{gdal_config_set()}}
}
