% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fgb_opts.R
\name{fgb_open_opts}
\alias{fgb_open_opts}
\title{FlatGeobuf Open Options}
\usage{
fgb_open_opts(verify_buffers = NULL, ..., .set_defaults = FALSE)
}
\arguments{
\item{verify_buffers}{Value for \code{VERIFY_BUFFERS}. Logical \code{TRUE}/\code{FALSE} (coerced to \code{"YES"}/
\code{"NO"}) controlling whether flatbuffer integrity is verified on read. \code{"YES"} (the GDAL default)
guards against corrupt data at a small performance cost; \code{"NO"} is faster but unsafe on
malformed files. \code{NULL} (default) leaves it unset.}

\item{...}{Additional \code{NAME = value} options passed through verbatim alongside the typed
arguments. They are coerced and validated against the driver metadata in the same way, and
take precedence over a typed argument that sets the same option.}

\item{.set_defaults}{Logical. If \code{TRUE}, options left unset (\code{NULL}) are filled with the driver's
documented GDAL metadata defaults (via the relevant \verb{gdal_vector_driver_*_opts_defaults()});
user-supplied values always take precedence. Defaults to \code{FALSE}.}
}
\value{
A \code{\link[=gdal_open_opts]{gdal_open_opts()}} object for the \code{FlatGeobuf} driver.
}
\description{
Construct a \code{\link[=gdal_open_opts]{gdal_open_opts()}} object for the \code{FlatGeobuf} driver. Only options you supply are
emitted; values are validated against the driver's registered metadata.
}
\examples{
fgb_open_opts()
fgb_open_opts(verify_buffers = FALSE)
fgb_open_opts(.set_defaults = TRUE)
}
\seealso{
\code{\link[=fgb_creation_opts]{fgb_creation_opts()}}, \code{\link[=gdal_open_opts]{gdal_open_opts()}}
\itemize{
\item \href{https://flatgeobuf.org/}{FlatGeobuf Home Page}
\item \href{https://gdal.org/en/stable/drivers/vector/flatgeobuf.html}{FlatGeobuf GDAL Driver}
\itemize{
\item \href{https://gdal.org/en/stable/drivers/vector/flatgeobuf.html#open-options}{FlatGeobuf GDAL Open Options}
\item \href{https://gdal.org/en/stable/drivers/vector/flatgeobuf.html#layer-creation-options}{FlatGeobuf GDAL Layer Creation Options}
}
}
}
