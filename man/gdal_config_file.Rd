% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_config_file.R
\name{gdal_config_file}
\alias{gdal_config_file}
\alias{gdal_config_file_read}
\alias{gdal_config_file_write}
\title{GDAL Configuration File}
\usage{
gdal_config_file_read(path)

gdal_config_file_write(x, path)

gdal_config_file(refresh = FALSE)
}
\arguments{
\item{path}{Path to a GDAL configuration file. For \code{gdal_config_file_write()}, the destination
(written with secrets as-is - it is a credentials file; printing remains redacted).}

\item{x}{A \code{\link[=gdal_config]{gdal_config()}} or \code{gdal_config_file} object to serialize.}

\item{refresh}{Logical; re-discover and re-parse instead of using the value cached at package
load. Defaults to \code{FALSE}.}
}
\value{
\itemize{
\item \code{gdal_config_file_read()} / \code{gdal_config_file()}: a \code{gdal_config_file} object (\code{path},
\code{configoptions}, \code{directives}, \code{credentials}). For \code{gdal_config_file()}, \code{path} is \code{NULL}
when no file was discovered.
\item \code{gdal_config_file_write()}: invisibly, the written path.
}
}
\description{
Read, write, and discover \href{https://gdal.org/en/stable/user/configoptions.html#gdal-configuration-file}{GDAL configuration files}
(gdalrc). A parsed file is a classed \code{gdal_config_file} whose components are the package's
option value classes, so a file round-trips directly into the configuration system:
\itemize{
\item \code{configoptions}: the \verb{[configoptions]} section as a \code{\link[=gdal_config_opts]{gdal_config_opts()}} (GDAL >= 3.3).
\item \code{directives}: the \verb{[directives]} section as a named character vector (GDAL >= 3.6;
e.g. \code{ignore-env-vars = "yes"}).
\item \code{credentials}: the \verb{[credentials]} section (GDAL >= 3.5) as a named list of
\code{\link[=gdal_vsi_opts]{gdal_vsi_opts()}} - each \verb{[.subsection]}'s \code{path} key becomes the \code{vsi_path} binding, so
path-bound credentials are first-class values.
}

Functions:
\itemize{
\item \code{gdal_config_file_read(path)}: parse a gdalrc.
\item \code{gdal_config_file_write(x, path)}: serialize a \code{\link[=gdal_config]{gdal_config()}} or \code{gdal_config_file} to a
gdalrc. This is the \strong{only lossless serialization} of a full configuration - path-bound VSI
options cannot be expressed as CLI \code{--config} flags (see \code{\link[=as_gdal_args]{as_gdal_args()}}).
\item \code{gdal_config_file(refresh)}: the file GDAL actually loaded at initialization, discovered via
the \code{GDAL_CONFIG_FILE} environment variable then the user gdalrc
(\verb{\%USERPROFILE\%/.gdal/gdalrc} on Windows, \verb{~/.gdal/gdalrc} otherwise), cached at package load.
\item \code{\link[=as_gdal_config]{as_gdal_config()}}: convert a \code{gdal_config_file} to a \code{\link[=gdal_config]{gdal_config()}} value
(configoptions + credentials), e.g. to apply it in-process via \code{\link[=gdal_config_set]{gdal_config_set()}}.
}

GDAL reads this file \strong{once}, at driver registration (i.e. before this package loads), and
loads its \verb{[configoptions]} into the in-memory configuration store (environment variables take
precedence over the file unless \code{ignore-env-vars=yes}). In-process it is therefore a
\emph{discovery/provenance} input - setting \code{GDAL_CONFIG_FILE} after load has no effect on the
running process (and \code{\link[=gdal_config_set]{gdal_config_set()}} warns if you try). Written files target \emph{future}
processes.
}
\examples{
\dontrun{
# the file GDAL loaded at startup, if any
gdal_config_file()

# serialize the session's configuration for reuse by CLI tools / other processes
gdal_config_file_write(gdal_config_active(), "gdalrc")
}
}
\seealso{
\code{\link[=gdal_config]{gdal_config()}}, \code{\link[=gdal_config_set]{gdal_config_set()}}, \code{\link[=gdal_config_sitrep]{gdal_config_sitrep()}}
}
