% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_vector_schema.R
\name{schema_spec_sql}
\alias{schema_spec_sql}
\title{Emit the SQLite-dialect SELECT from a Schema Spec}
\usage{
schema_spec_sql(spec, layer, where = NULL, exclude_empty_geom = TRUE)
}
\arguments{
\item{spec}{A \code{\link[=gdal_vector_schema_spec]{gdal_vector_schema_spec()}}.}

\item{layer}{The source layer name for the \code{FROM} clause.}

\item{where}{Optional additional \code{WHERE} clause (without the \code{WHERE} keyword).}

\item{exclude_empty_geom}{Logical; if \code{TRUE} (default) adds \verb{NOT ST_IsEmpty(<geom>)} to the \code{WHERE} clause.}
}
\value{
A single character string containing the SQL statement.
}
\description{
Builds the \code{SELECT} statement (OGR SQLite dialect) that renames and casts source fields into the target schema.
Attribute fields become \verb{CAST(<source> AS <sql_type>) AS <target>}, the synthetic FID row becomes
\verb{CAST(rowid AS INTEGER) AS <target>}, and the geometry field becomes \verb{<source> AS <target>} (no cast).
}
