% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpkg_opts.R
\name{gpkg_creation_opts}
\alias{gpkg_creation_opts}
\title{GeoPackage Creation Options}
\usage{
gpkg_creation_opts(
  fid = NULL,
  geometry_name = NULL,
  geometry_nullable = NULL,
  spatial_index = NULL,
  identifier = NULL,
  description = NULL,
  launder = NULL,
  overwrite = NULL,
  ...,
  level = c("layer", "dataset"),
  .set_defaults = FALSE
)
}
\arguments{
\item{fid}{Name of the FID column (\code{FID}). GDAL default \code{"fid"}.}

\item{geometry_name}{Name of the geometry column (\code{GEOMETRY_NAME}). GDAL default \code{"geom"}.}

\item{geometry_nullable}{Value for \code{GEOMETRY_NULLABLE} (logical -> \code{"YES"}/\code{"NO"}).}

\item{spatial_index}{Value for \code{SPATIAL_INDEX} (logical -> \code{"YES"}/\code{"NO"}). GDAL default \code{"YES"}.}

\item{identifier}{Value for \code{IDENTIFIER} (contents-table identifier).}

\item{description}{Value for \code{DESCRIPTION} (contents-table description).}

\item{launder}{Value for \code{LAUNDER} (logical -> \code{"YES"}/\code{"NO"}). GDAL default \code{"NO"}.}

\item{overwrite}{Value for \code{OVERWRITE} (logical -> \code{"YES"}/\code{"NO"}). GDAL default \code{"NO"}.}

\item{...}{Additional \code{NAME = value} options passed through verbatim alongside the typed
arguments. They are coerced and validated against the driver metadata in the same way, and
take precedence over a typed argument that sets the same option.}

\item{level}{Creation-option level, \code{"layer"} (default, \code{--lco}) or \code{"dataset"} (\code{--co}).
Dataset-level options (e.g. \code{VERSION}, \code{METADATA_TABLES}, \code{ADD_GPKG_OGR_CONTENTS}) are supplied
through \code{...} with \code{level = "dataset"}.}

\item{.set_defaults}{Logical. If \code{TRUE}, options left unset (\code{NULL}) are filled with the driver's
documented GDAL metadata defaults (via the relevant \verb{gdal_vector_driver_*_opts_defaults()});
user-supplied values always take precedence. Defaults to \code{FALSE}.}
}
\value{
A \code{\link[=gdal_creation_opts]{gdal_creation_opts()}} object for the \code{GPKG} driver.
}
\description{
Construct a \code{\link[=gdal_creation_opts]{gdal_creation_opts()}} object for the \code{GPKG} driver. The typed arguments are
layer-creation options (\code{level = "layer"}, the default, \code{--lco}); dataset-creation options
(\code{VERSION}, \code{METADATA_TABLES}, \code{ADD_GPKG_OGR_CONTENTS}, ...) are supplied through \code{...} together
with \code{level = "dataset"}.
}
\examples{
gpkg_creation_opts(geometry_name = "geom", spatial_index = TRUE)
gpkg_creation_opts(VERSION = "1.4", level = "dataset")
}
\seealso{
\code{\link[=gpkg_open_opts]{gpkg_open_opts()}}, \code{\link[=gdal_creation_opts]{gdal_creation_opts()}}
\itemize{
\item \href{https://gdal.org/en/stable/drivers/vector/gpkg.html}{GDAL GPKG (GeoPackage) vector driver}
\item \href{https://gdal.org/en/stable/drivers/vector/gpkg.html#open-options}{GPKG open options}
\item \href{https://gdal.org/en/stable/user/configoptions.html}{GDAL configuration options}
\item \href{https://www.sqlite.org/pragma.html}{SQLite \code{PRAGMA} statements}
\item \href{https://www.geopackage.org/spec/}{GeoPackage specification}
}
}
