% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_vector_schema.R
\name{gdal_vector_schema_pipeline_args}
\alias{gdal_vector_schema_pipeline_args}
\title{Emit the Schema Pipeline Steps from a Spec}
\usage{
gdal_vector_schema_pipeline_args(
  spec,
  layer,
  where = NULL,
  exclude_empty_geom = TRUE,
  dialect = "SQLITE",
  make_valid = FALSE,
  set_field_type_policy = c("nonstring_keys", "all_nonstring", "all", "retyped"),
  force_keys = character(),
  multi = TRUE,
  skip = TRUE
)
}
\arguments{
\item{spec}{A \code{\link[=gdal_vector_schema_spec]{gdal_vector_schema_spec()}}.}

\item{layer}{The source layer name (for the SQL \code{FROM} clause and \code{select}).}

\item{where, exclude_empty_geom}{See \code{\link[=schema_spec_sql]{schema_spec_sql()}}.}

\item{dialect}{The SQL dialect for the \code{sql} step. Defaults to \code{"SQLITE"}.}

\item{make_valid}{Logical; insert a \code{make-valid} step after the projection. Defaults to \code{FALSE}.}

\item{set_field_type_policy, force_keys}{See \code{\link[=schema_spec_set_field_type_args]{schema_spec_set_field_type_args()}}.}

\item{multi, skip}{See \code{\link[=schema_spec_set_geom_type_args]{schema_spec_set_geom_type_args()}}.}
}
\value{
A flat character vector of pipeline arguments.
}
\description{
The top-level decision engine. Given a (possibly edited) \code{\link[=gdal_vector_schema_spec]{gdal_vector_schema_spec()}}, it emits the minimal set of
\verb{gdal vector pipeline} steps required to realize the target schema, ready to be spliced between a \code{read} and a
\code{write} step (each step is prefixed with the \code{!} connector). The emitted steps, in order, are:
\enumerate{
\item Either \verb{select --fields ...} (subset only) or \verb{sql --sql ... --dialect ...} (rename / FID / filter), per
\code{\link[=schema_spec_plan]{schema_spec_plan()}}. For the \code{select} path a separate \verb{filter --where ...} step is emitted first when a
filter is needed.
\item \code{make-valid} (optional, off by default).
\item \code{set-geom-type --multi --skip} when a geometry is kept.
\item \code{set-field-type} steps to pin output types (see \code{\link[=schema_spec_set_field_type_args]{schema_spec_set_field_type_args()}}).
}
}
\examples{
\dontrun{
spec <- gdal_vector_schema_spec(dsn)
# ... edit target_name / target_type / keep ...
args <- gdal_vector_schema_pipeline_args(
  spec,
  layer = gpkg_layer,
  force_keys = c("geoid", "state_fips", "county_fips")
)
gdalraster::gdal_run("vector pipeline", args = c("read", "--input", dsn, args, "write", "--output", out))
}
}
