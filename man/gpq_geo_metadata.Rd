% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpq_meta.R
\name{gpq_geo_metadata}
\alias{gpq_geo_metadata}
\title{GeoParquet and GDAL Metadata}
\usage{
gpq_geo_metadata(gpq_path)
}
\arguments{
\item{gpq_path}{Path to a (Geo)Parquet (\verb{*.parquet}) file.}
}
\value{
A \code{gpq_geo_metadata} object (a list). When no \code{geo} metadata is present, \code{is_geoparquet} is \code{FALSE} and the
geospatial fields are omitted.
}
\description{
Read and parse the GeoParquet (\code{geo}) and GDAL (\code{gdal:creation-options}, \code{gdal:schema}) key-value metadata, plus
any \code{pandas} metadata. Geometry column metadata is accessed via the declared \code{primary_column}, and the \code{crs} field
is normalized to a \code{type}/\code{name}/\code{authority} summary across its three forms: absent (\code{NULL}), a PROJJSON object,
or a WKT string.
}
\examples{
f <- system.file("extdata/atlanta/atlanta.parquet", package = "gdalvector")
gpq_geo_metadata(f)
}
