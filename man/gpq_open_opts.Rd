% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpq_opts.R
\name{gpq_open_opts}
\alias{gpq_open_opts}
\title{GeoParquet Open Options}
\usage{
gpq_open_opts(
  geom_possible_names = NULL,
  crs = NULL,
  lists_as_string_json = NULL,
  ...,
  .set_defaults = FALSE
)
}
\arguments{
\item{geom_possible_names}{Value for \code{GEOM_POSSIBLE_NAMES} (GDAL >= 3.8). Comma-separated list of
candidate geometry column names, used only for files without GeoParquet metadata. GDAL default
\code{"geometry,wkb_geometry,wkt_geometry"}.}

\item{crs}{Value for \code{CRS} (GDAL >= 3.8). Set or override the CRS of geometry columns, typically
\code{"AUTH:CODE"} (e.g. \code{"EPSG:4326"}), or a PROJ/WKT CRS string.}

\item{lists_as_string_json}{Value for \code{LISTS_AS_STRING_JSON} (GDAL >= 3.12.1; logical ->
\code{"YES"}/\code{"NO"}). Report lists of strings/integers/reals as \code{String(JSON)} fields. GDAL default
\code{"NO"}.}

\item{...}{Additional \code{NAME = value} options passed through verbatim alongside the typed
arguments. They are coerced and validated against the driver metadata in the same way, and
take precedence over a typed argument that sets the same option.}

\item{.set_defaults}{Logical. If \code{TRUE}, options left unset (\code{NULL}) are filled with the driver's
documented GDAL metadata defaults (via the relevant \verb{gdal_vector_driver_*_opts_defaults()});
user-supplied values always take precedence. Defaults to \code{FALSE}.}
}
\value{
A \code{\link[=gdal_open_opts]{gdal_open_opts()}} object for the \code{Parquet} driver.
}
\description{
Construct a \code{\link[=gdal_open_opts]{gdal_open_opts()}} object for the \code{Parquet} (GeoParquet) driver.
}
\examples{
gpq_open_opts(crs = "EPSG:4326")
}
\seealso{
\code{\link[=gpq_creation_opts]{gpq_creation_opts()}}, \code{\link[=gdal_open_opts]{gdal_open_opts()}}
\itemize{
\item \href{https://geoparquet.org/}{GeoParquet Home Page}
\item \href{https://gdal.org/en/stable/drivers/vector/parquet.html}{(Geo)Parquet GDAL Driver}
\itemize{
\item \href{https://gdal.org/en/stable/drivers/vector/parquet.html#layer-creation-options}{(Geo)Parquet GDAL Layer Creation Options}
\item \href{https://gdal.org/en/stable/drivers/vector/parquet.html#open-options}{(Geo)Parquet GDAL Open Options}
}
\item \href{https://github.com/opengeospatial/geoparquet/blob/main/format-specs/distributing-geoparquet.md}{Best Practices for Distributing GeoParquet}
}
}
