% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpq_meta.R
\name{gpq_schema}
\alias{gpq_schema}
\title{GeoParquet Schema}
\usage{
gpq_schema(gpq_path)
}
\arguments{
\item{gpq_path}{Path to a (Geo)Parquet (\verb{*.parquet}) file.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{id}, \code{name}, \code{parquet_type}, \code{converted_type}, \code{logical_type}, \code{repetition},
\code{r_type}, and \code{info}.
}
\description{
Read the tidied Parquet schema as a tibble: one row per leaf column with its Parquet physical type,
converted/logical types, repetition, mapped R type, and (for geometry columns) the encoding and geometry types
from the \code{geo} metadata. For a high-level summary instead of the full table, see \code{\link[=gpq_schema_info]{gpq_schema_info()}}.
}
\examples{
f <- system.file("extdata/atlanta/atlanta.parquet", package = "gdalvector")
gpq_schema(f)
}
