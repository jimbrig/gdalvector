% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpq_meta.R
\name{gpq_file_info}
\alias{gpq_file_info}
\title{GeoParquet File Info}
\usage{
gpq_file_info(gpq_path)
}
\arguments{
\item{gpq_path}{Path to a (Geo)Parquet (\verb{*.parquet}) file.}
}
\value{
A \code{gpq_file_info} object (a list) with:
\itemize{
\item \code{source}, \code{path}: the file name and full path.
\item \code{file_size}: the file size in bytes.
\item \code{parquet_version}, \code{geoparquet_version}: the format versions (the latter is \code{NA} for plain Parquet).
\item \code{num_rows}, \code{num_cols}, \code{num_row_groups}: the row, column, and row group counts.
\item \code{row_group_size}, \code{row_group_size_last}: the number of rows in the first and last row groups.
\item \code{created_by}: the writing software.
\item \code{kv_keys}: the key-value metadata keys present in the file.
}
}
\description{
Summarize file-level information about a (Geo)Parquet file from its footer.
}
\examples{
f <- system.file("extdata/atlanta/atlanta.parquet", package = "gdalvector")
gpq_file_info(f)
}
