% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpkg_opts.R
\name{gpkg_config_opts}
\alias{gpkg_config_opts}
\title{GeoPackage Configuration Options}
\usage{
gpkg_config_opts(
  sqlite_cache = NULL,
  sqlite_journal = NULL,
  sqlite_synchronous = NULL,
  sqlite_pragma = NULL,
  use_ogr_vfs = NULL,
  num_threads = NULL,
  ...,
  .set_defaults = FALSE
)
}
\arguments{
\item{sqlite_cache}{Value for \code{OGR_SQLITE_CACHE} (SQLite page cache, in MB).}

\item{sqlite_journal}{Value for \code{OGR_SQLITE_JOURNAL} (journal mode).}

\item{sqlite_synchronous}{Value for \code{OGR_SQLITE_SYNCHRONOUS} (e.g. \code{"OFF"}).}

\item{sqlite_pragma}{Value for \code{OGR_SQLITE_PRAGMA} (e.g. \code{"pragma_name=value,..."}).}

\item{use_ogr_vfs}{Value for \code{SQLITE_USE_OGR_VFS} (logical -> \code{"YES"}/\code{"NO"}).}

\item{num_threads}{Value for \code{OGR_GPKG_NUM_THREADS} (GDAL >= 3.8.3); an integer or \code{"ALL_CPUS"}.
Threads used when reading tables through the ArrowArray interface. GDAL default is \code{min(4, nCPU)}.}

\item{...}{Additional \code{NAME = value} options passed through verbatim alongside the typed
arguments. They are coerced and validated against the driver metadata in the same way, and
take precedence over a typed argument that sets the same option.}

\item{.set_defaults}{Logical. If \code{TRUE}, options left unset (\code{NULL}) are filled with the driver's
documented GDAL metadata defaults (via the relevant \verb{gdal_vector_driver_*_opts_defaults()});
user-supplied values always take precedence. Defaults to \code{FALSE}.}
}
\value{
A \code{\link[=gdal_config_opts]{gdal_config_opts()}} object for the \code{GPKG} driver.
}
\description{
Construct a \code{\link[=gdal_config_opts]{gdal_config_opts()}} object for the \code{GPKG} driver. These are global configuration options
applied to the GDAL process.
}
\examples{
gpkg_config_opts(sqlite_synchronous = "OFF", use_ogr_vfs = TRUE, num_threads = "ALL_CPUS")
}
\seealso{
\code{\link[=gpkg_open_opts]{gpkg_open_opts()}}, \code{\link[=gpkg_creation_opts]{gpkg_creation_opts()}}, \code{\link[=gdal_config_opts]{gdal_config_opts()}}
\itemize{
\item \href{https://gdal.org/en/stable/drivers/vector/gpkg.html}{GDAL GPKG (GeoPackage) vector driver}
\item \href{https://gdal.org/en/stable/drivers/vector/gpkg.html#open-options}{GPKG open options}
\item \href{https://gdal.org/en/stable/user/configoptions.html}{GDAL configuration options}
\item \href{https://www.sqlite.org/pragma.html}{SQLite \code{PRAGMA} statements}
\item \href{https://www.geopackage.org/spec/}{GeoPackage specification}
}
}
