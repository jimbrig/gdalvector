% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_vector_schema.R
\name{schema_spec_set_field_type_args}
\alias{schema_spec_set_field_type_args}
\title{Emit \code{set-field-type} Pipeline Steps from a Schema Spec}
\usage{
schema_spec_set_field_type_args(
  spec,
  policy = c("nonstring_keys", "all_nonstring", "all", "retyped"),
  force_keys = character()
)
}
\arguments{
\item{spec}{A \code{\link[=gdal_vector_schema_spec]{gdal_vector_schema_spec()}}.}

\item{policy}{Which fields receive an explicit step:
\describe{
\item{\code{"nonstring_keys"}}{(default) non-\code{String} fields plus any \code{force_keys}.}
\item{\code{"all_nonstring"}}{all non-\code{String} fields.}
\item{\code{"all"}}{every kept attribute/FID field.}
\item{\code{"retyped"}}{only fields whose \code{target_type} differs from \code{source_type}.}
}}

\item{force_keys}{Character vector of target field names to always include (used by the \code{"nonstring_keys"}
policy, e.g. FIPS/\code{geoid} string keys that must be pinned as \code{String}).}
}
\value{
A flat character vector of pipeline arguments (empty if nothing qualifies).
}
\description{
Builds the flat character vector of \verb{! set-field-type --field-name <f> --field-type <t>} steps used to pin
field types before a (Geo)Parquet write. This is necessary because a \code{CAST} in SQL is not honored on output for
(Geo)Parquet: an all-\code{NULL} column in a filtered slice is inferred as \code{String}, and populated integers come back
as 32-bit \code{Integer}.
}
