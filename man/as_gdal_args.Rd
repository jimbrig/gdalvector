% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_options.R
\name{as_gdal_args}
\alias{as_gdal_args}
\alias{as_gdal_args.gdal_opts}
\alias{as_gdal_args.character}
\alias{as_gdal_args.list}
\alias{as_gdal_args.default}
\title{Convert GDAL Options to Algorithm Arguments}
\usage{
as_gdal_args(x, ...)

\method{as_gdal_args}{gdal_opts}(x, cli = TRUE, long = FALSE, with_format = FALSE, ...)

\method{as_gdal_args}{character}(x, ...)

\method{as_gdal_args}{list}(x, ...)

\method{as_gdal_args}{default}(x, ...)
}
\arguments{
\item{x}{A \code{\link[=gdal_opts]{gdal_opts()}} object (or a character vector / list, passed through).}

\item{...}{Passed to methods.}

\item{cli}{Logical; emit interleaved CLI tokens (\code{TRUE}, default) or a bare \code{KEY=VALUE} vector.}

\item{long}{Logical; use long flag names (\code{--open-option}) rather than aliases (\code{--oo}).}

\item{with_format}{Logical; prepend the \code{--input-format}/\code{--output-format} flag and driver when
known (open/creation only).}
}
\value{
A character vector.
}
\description{
Render a \code{\link[=gdal_opts]{gdal_opts()}} object to the form consumed by the GDAL algorithm API
(\code{\link[gdalraster:gdal_alg]{gdalraster::gdal_alg()}} / \code{\link[gdalraster:gdal_run]{gdalraster::gdal_run()}}).
}
\details{
For repeated options (\code{--oo}/\code{--co}/\code{--lco}), GDAL requires each value to be preceded by its own
flag - values are never comma-packed (a packed value would corrupt options such as
\code{PRELUDE_STATEMENTS} that themselves contain \verb{;}/\verb{,}). Accordingly:
\itemize{
\item \code{cli = TRUE} (default) emits a flat token vector \code{c("--open-option", "K=V", "--open-option", "K2=V2", ...)}, suitable as the \code{args} to \code{\link[gdalraster:gdal_alg]{gdalraster::gdal_alg()}}.
\item \code{cli = FALSE} emits an unnamed \code{c("K=V", ...)} vector, suitable for a single
\verb{alg$setArg(<flag>, .)} call.
}
}
\examples{
as_gdal_args(gdal_open_opts(LIST_ALL_TABLES = FALSE, driver = "GPKG"))
as_gdal_args(gdal_open_opts(LIST_ALL_TABLES = FALSE), cli = FALSE)
}
