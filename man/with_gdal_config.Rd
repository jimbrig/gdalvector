% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_config.R
\name{with_gdal_config}
\alias{with_gdal_config}
\alias{local_gdal_config}
\title{Scoped GDAL Configuration}
\usage{
local_gdal_config(..., .local_envir = parent.frame())

with_gdal_config(new, code)
}
\arguments{
\item{...}{Configuration options: \code{KEY = value} pairs, \code{\link[=gdal_config_opts]{gdal_config_opts()}} / \code{\link[=gdal_vsi_opts]{gdal_vsi_opts()}}
objects, named lists, \code{"KEY=VALUE"} character vectors, or \code{gdal_config} values. Open/creation
options are rejected - they are per-call algorithm arguments (see \code{\link[=as_gdal_args]{as_gdal_args()}}), never
configuration.}

\item{.local_envir}{Environment whose exit triggers restoration. Defaults to the calling frame.}

\item{new}{Configuration options: a \code{\link[=gdal_config]{gdal_config()}} value or anything \code{\link[=gdal_config]{gdal_config()}} accepts.}

\item{code}{Code to execute with the configuration in place.}
}
\value{
\itemize{
\item \code{local_gdal_config()}: invisibly, a named list of the prior effective values.
\item \code{with_gdal_config()}: the value of \code{code}.
}
}
\description{
Apply GDAL configuration options for a limited scope and guarantee restoration afterwards -
even on error. This is the recommended pattern for workloads: the configuration channel is the
\emph{execution environment} of a pipeline run (it cannot ride in algorithm arguments or a serialized
GDALG), so it should be pinned in place around the execution and cleaned up after.
\itemize{
\item \code{local_gdal_config()}: applies the options now and restores them when the calling frame (or
\code{.local_envir}) exits, in the style of the withr \verb{local_*()} functions.
\item \code{with_gdal_config()}: applies the options, evaluates \code{code}, restores, and returns the value
of \code{code}.
}

Restoration is exact: the effective GDAL values \emph{and} the package state are returned to their
condition at entry, so scoped usage never disturbs global configuration set via
\code{\link[=gdal_config_set]{gdal_config_set()}}. For VSI path-scoped options the prior value cannot be read back from GDAL,
so keys not previously set by the package are cleared on exit.
}
\examples{
\dontrun{
# pin the execution environment around a pipeline run; restored even on error
with_gdal_config(
  gdal_config(GDAL_NUM_THREADS = "ALL_CPUS", OGR_SQLITE_SYNCHRONOUS = "OFF"),
  gdalraster::gdal_run("vector convert", c("--input", "in.gpkg", "--output", "out.parquet"))
)

# the CPL_DEBUG/GeoParquet guard (see the config docs) is just:
with_gdal_config(gdal_config(CPL_DEBUG = "OFF"), gdal_vector_info("data.parquet"))
}
}
\seealso{
\code{\link[=gdal_config]{gdal_config()}}, \code{\link[=gdal_config_set]{gdal_config_set()}}
}
