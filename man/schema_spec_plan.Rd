% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_vector_schema.R
\name{schema_spec_plan}
\alias{schema_spec_plan}
\title{Plan the Schema Transformation from a Spec}
\usage{
schema_spec_plan(spec, where = NULL, exclude_empty_geom = TRUE)
}
\arguments{
\item{spec}{A \code{\link[=gdal_vector_schema_spec]{gdal_vector_schema_spec()}}.}

\item{where, exclude_empty_geom}{See \code{\link[=schema_spec_sql]{schema_spec_sql()}}. A non-\code{NULL} \code{where} (or \code{exclude_empty_geom = TRUE} with
a geometry present) forces the \code{"sql"} method.}
}
\value{
A list with logical flags (\code{rename}, \code{retype}, \code{drop}, \code{fid}, \code{filter}) and the chosen \code{method}.
}
\description{
Inspects the diff between the source and target columns of a \code{\link[=gdal_vector_schema_spec]{gdal_vector_schema_spec()}} and reports which
transformation kinds are present and the resulting pipeline \code{method}:
\itemize{
\item \code{"sql"}: required when any field is renamed, the synthetic FID is kept, or a \code{where}/empty-geometry filter must
ride along with the projection.
\item \code{"select"}: when fields are only being subset (dropped) - emits \code{select --fields}.
\item \code{"none"}: a pure pass-through (all fields kept, no rename).
}

Type changes are handled by \code{set-field-type} regardless of method, so they do not by themselves force SQL.
}
