% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpq_meta.R
\name{gpq_arrow_schema}
\alias{gpq_arrow_schema}
\title{GeoParquet Arrow Schema}
\usage{
gpq_arrow_schema(gpq_path)
}
\arguments{
\item{gpq_path}{Path to a (Geo)Parquet (\verb{*.parquet}) file.}
}
\value{
A \code{gpq_arrow_schema} object (a list) with:
\itemize{
\item \code{source}, \code{path}: the file name and full path.
\item \code{fields}: a \code{\link[tibble:tibble]{tibble::tibble()}} of \code{index}, \code{name}, \code{arrow_type}, \code{nullable}, and \code{extension}.
\item \code{n_fields}: the field count.
\item \code{schema_meta}: the schema-level key-value metadata.
\item \code{schema}: the raw \link[arrow:Schema]{arrow::Schema} object.
}
}
\description{
Read the Arrow-level schema (Arrow data types and extension metadata such as \code{geoarrow.wkb}). Requires the
\pkg{arrow} package and aborts with an installation hint when it is not available.
}
\examples{
\dontrun{
f <- system.file("extdata/atlanta/atlanta.parquet", package = "gdalvector")
gpq_arrow_schema(f)
}
}
