% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpq_meta.R
\name{gpq_row_groups}
\alias{gpq_row_groups}
\title{GeoParquet Row Groups and Statistics}
\usage{
gpq_row_groups(gpq_path)
}
\arguments{
\item{gpq_path}{Path to a (Geo)Parquet (\verb{*.parquet}) file.}
}
\value{
A \code{gpq_row_groups} object (a list) with:
\itemize{
\item \code{source}, \code{path}: the file name and full path.
\item \code{row_groups}: a per-row-group \code{\link[tibble:tibble]{tibble::tibble()}}.
\item \code{column_chunks}: the raw per-chunk \code{\link[tibble:tibble]{tibble::tibble()}} with leaf column names and decoded min/max appended.
\item \code{col_summary}: a one-row-per-column \code{\link[tibble:tibble]{tibble::tibble()}} aggregated across all row groups.
}
}
\description{
Read row group and column chunk statistics. Parquet stores chunk min/max statistics as raw bytes in the column's
physical type; these are decoded to R scalars and aggregated into a per-column summary across all row groups.
}
\examples{
f <- system.file("extdata/atlanta/atlanta.parquet", package = "gdalvector")
gpq_row_groups(f)
}
