% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_drivers.R
\name{gdal_drivers}
\alias{gdal_drivers}
\alias{gdal_driver_names}
\title{GDAL Drivers}
\usage{
gdal_drivers(pattern = NULL, ignore_case = TRUE)

gdal_driver_names(pattern = NULL)
}
\arguments{
\item{pattern}{Optional character vector of regular-expression patterns. Drivers whose short or
long name matches any pattern are returned. \code{NULL} (default) returns all drivers.}

\item{ignore_case}{Logical; match \code{pattern} case-insensitively. Defaults to \code{TRUE}.}
}
\value{
\itemize{
\item \code{gdal_drivers()}: a \code{\link[tibble:tibble]{tibble::tibble()}} with one row per driver.
\item \code{gdal_driver_names()}: a character vector of short driver names.
}
}
\description{
Query the GDAL drivers registered in the active GDAL build.
\itemize{
\item \code{gdal_drivers()}: returns a normalized \code{\link[tibble:tibble]{tibble::tibble()}} of driver metadata (identity,
capabilities, supported extensions and SQL dialects), optionally filtered by a name pattern.
\item \code{gdal_driver_names()}: returns just the short driver names.
}

The driver table is built once from \code{\link[gdalraster:gdal_formats]{gdalraster::gdal_formats()}} and cached for the session.
}
\examples{
gdal_drivers()
gdal_drivers(c("parquet", "geojson"))
gdal_driver_names("gpkg")
}
\seealso{
\code{\link[=gdal_vector_driver_opts]{gdal_vector_driver_opts()}}, \code{\link[=gdal_vector_driver_capabilities]{gdal_vector_driver_capabilities()}}
}
