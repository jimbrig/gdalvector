% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_config.R
\name{gdal_config_option_known}
\alias{gdal_config_option_known}
\title{Check Configuration Option Names Against the Running GDAL Build}
\usage{
gdal_config_option_known(keys)
}
\arguments{
\item{keys}{Character vector of configuration option names.}
}
\value{
A named logical vector: \code{TRUE} when the running build knows the key, \code{FALSE} when it does not,
\code{NA} when detection is unavailable (GDAL < 3.11).
}
\description{
Ask the \emph{running} GDAL build whether it knows a configuration option name. GDAL >= 3.11 emits a
warning from \code{CPLSetConfigOption()} for unknown option names when \code{CPL_DEBUG} is enabled; this
helper performs a no-op set of each key under a temporarily enabled \code{CPL_DEBUG} and captures
that signal, then restores all touched state. This is the runtime ground truth that
\code{CPLGetKnownConfigOptions()} would provide if gdalraster exposed it - exact for the loaded
build, unaffected by version drift in the generated known-option lists.

Note that "unknown to GDAL" does not mean invalid: setting an unknown option is legal and
silently ignored (which is exactly why typo detection matters). Used internally to confirm
suspected-unknown keys before \code{\link[=gdal_config_set]{gdal_config_set()}} raises its advisory warning.
}
\examples{
\dontrun{
gdal_config_option_known(c("GDAL_NUM_THREADS", "GDAL_NUM_THREDS"))
}
}
\seealso{
\code{\link[=gdal_config_set]{gdal_config_set()}}, \code{\link[=gdal_config_sitrep]{gdal_config_sitrep()}}
}
