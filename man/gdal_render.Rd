% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_options.R
\name{gdal_render}
\alias{gdal_render}
\title{Render GDAL Options as a Shell Command Snippet}
\usage{
gdal_render(x, shell = c("bash", "sh", "pwsh", "cmd"))
}
\arguments{
\item{x}{A \code{\link[=gdal_opts]{gdal_opts()}} object.}

\item{shell}{Target shell dialect controlling quoting and continuation: \code{"bash"}/\code{"sh"}
(\verb{\\\\}, single quotes), \code{"pwsh"} (\verb{`}, single quotes), or \code{"cmd"} (\code{^}, double quotes).}
}
\value{
A length-1 character string (embedded newlines), or \code{""} when there are no options.
}
\description{
Render a \code{\link[=gdal_opts]{gdal_opts()}} object to a copy-pasteable, multi-line shell snippet with one
flag/value pair per line and the appropriate line-continuation for the target shell. Only the
option flags (and the leading \code{--input-format}/\code{--output-format} when the driver is known) are
rendered; the base \code{gdal} invocation and datasets are not included.
}
\examples{
gdal_render(gdal_creation_opts(COMPRESSION = "ZSTD", driver = "Parquet"), shell = "bash")
}
