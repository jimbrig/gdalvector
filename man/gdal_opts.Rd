% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_options.R
\name{gdal_opts}
\alias{gdal_opts}
\title{GDAL Options}
\description{
GDAL options control how the GDAL/OGR library reads, writes, and otherwise processes geospatial
data. This package models the distinct GDAL option \emph{channels} as a small family of S3 classes,
each backed by a named list of \code{NAME = "VALUE"} pairs (values stored as their coerced GDAL
strings) plus a \code{driver} attribute, so a set of options is an inert, composable value that can be
rendered on demand to whichever form a given consumer needs.

In the GDAL CLI, an option is supplied via one of these argument flags:

\if{html}{\out{<div class="sourceCode plaintext">}}\preformatted{--config <NAME>=<VALUE>
--open-option / --oo <NAME>=<VALUE>
--creation-option / --co <NAME>=<VALUE>
--layer-creation-option / --lco <NAME>=<VALUE>
}\if{html}{\out{</div>}}
}
\section{Classes}{

All four classes share the \code{gdal_opts} base (a named \code{list}) and differ only by channel:

\if{html}{\out{<div class="sourceCode plaintext">}}\preformatted{gdal_opts
 |- gdal_config_opts                          --config      CPLSetConfigOption()
 |- gdal_open_opts                            --oo          GDALOpenEx()
 |- gdal_creation_opts  (level = layer)       --lco         GDALDatasetCreateLayer()
 |                      (level = dataset)     --co          GDALCreate()
 |- gdal_vsi_opts       (vsi_path)            --config      VSISetPathSpecificOption()
}\if{html}{\out{</div>}}
\itemize{
\item \code{\link[=gdal_config_opts]{gdal_config_opts()}}: global, stateful configuration options. Applied to the process/session
(via \code{\link[gdalraster:set_config_option]{gdalraster::set_config_option()}}), not as an algorithm argument.
\item \code{\link[=gdal_open_opts]{gdal_open_opts()}}: driver open options.
\item \code{\link[=gdal_creation_opts]{gdal_creation_opts()}}: driver dataset- or layer-creation options, selected by \code{level}.
\item \code{\link[=gdal_vsi_opts]{gdal_vsi_opts()}}: virtual file system (VSI) path-scoped options (config-like).
}
}

\section{Rendering}{

A \code{gdal_opts} value is rendered with \code{\link[=as_gdal_args]{as_gdal_args()}} (CLI token vector for
\code{\link[gdalraster:gdal_alg]{gdalraster::gdal_alg()}} / \code{\link[gdalraster:gdal_run]{gdalraster::gdal_run()}}), \code{\link[=as_config_option]{as_config_option()}} (a \code{NAME = VALUE}
character vector for \code{\link[gdalraster:set_config_option]{gdalraster::set_config_option()}}, config/VSI only), or \code{\link[=gdal_render]{gdal_render()}}
(a copy-pasteable shell snippet).
}

\seealso{
\code{\link[=as_gdal_args]{as_gdal_args()}}, \code{\link[=as_config_option]{as_config_option()}}, \code{\link[=gdal_render]{gdal_render()}}; and the typed per-driver builders
\code{\link[=gpkg_open_opts]{gpkg_open_opts()}}, \code{\link[=gpq_creation_opts]{gpq_creation_opts()}}, \code{\link[=shp_open_opts]{shp_open_opts()}}, \code{\link[=fgb_creation_opts]{fgb_creation_opts()}},
\code{\link[=gdb_open_opts]{gdb_open_opts()}}.
}
