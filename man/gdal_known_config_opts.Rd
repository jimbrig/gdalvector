% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_config.R
\name{gdal_known_config_opts}
\alias{gdal_known_config_opts}
\title{Known GDAL Configuration Options}
\usage{
gdal_known_config_opts(pattern = NULL)
}
\arguments{
\item{pattern}{Optional character vector of regular-expression patterns; options whose \code{name}
or \code{source} matches any pattern (case-insensitively) are returned. \code{NULL} (default) returns
all options.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{name} (option name) and \code{source} (the GDAL source file that
consumes it), carrying a \code{gdal_version} attribute with the header's version pin.
}
\description{
GDAL's compile-time known-configuration-option registry: the contents of
\code{port/cpl_known_config_options.h} in the GDAL source tree (generated upstream by
\code{collect_config_options.py}), which is the exact array \code{CPLGetKnownConfigOptions()}
(GDAL >= 3.11) returns. gdalraster does not bind that function, so the package parses the
version-pinned source header instead (\code{data-raw/scripts/gdal_known_config_options.R}, which
documents the pinned URL); a copy of the header is also bundled under \verb{inst/schemas/} for
transparency (see \code{inst/schemas/schemas.R}).

The pinned GDAL version is recorded in the \code{gdal_version} attribute of the returned tibble;
drift between that pin and the \emph{running} GDAL build is covered at option-checking time by the
per-key runtime confirmation of \code{\link[=gdal_config_option_known]{gdal_config_option_known()}}.
}
\examples{
gdal_known_config_opts()
gdal_known_config_opts("SQLITE")
attr(gdal_known_config_opts(), "gdal_version")
}
\seealso{
\code{\link[=gdal_config_option_known]{gdal_config_option_known()}}, \code{\link[=gdal_config_set]{gdal_config_set()}}, \code{\link[=gdal_config_sitrep]{gdal_config_sitrep()}}
}
