# Get the Size of a VSI Path

Get the Size of a VSI Path

## Usage

``` r
vsi_size(path)
```

## Arguments

- path:

  Path to inspect.

## Value

The size of the file/object in bytes, formatted using
[`rlang::as_bytes()`](https://rlang.r-lib.org/reference/bytes-class.html).
