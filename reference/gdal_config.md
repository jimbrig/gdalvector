# GDAL Configuration

Creates a session-wide, global GDAL Configuration object of class
`"gdal_config"`.

## Usage

``` r
gdal_config(...)
```

## Arguments

- ...:

  Configuration options as named `KEY = value` pairs.

## Value

gdal_config
