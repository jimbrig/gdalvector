# Ping

Pings a remote URL.

## Usage

``` r
ping(url, timeout = 5L)
```

## Arguments

- url:

  The remote URL

- timeout:

  Timeout in seconds. Defaults to `5L`.

## Value

`TRUE` if successfully pinged, `FALSE` otherwise.
